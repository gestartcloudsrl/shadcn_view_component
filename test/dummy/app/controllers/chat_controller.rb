# frozen_string_literal: true

# A chat built from the ported components, at `/chat`. The Lookbook preview
# shows the message scroller on its own; this shows it doing the job it exists
# for — inside a card, between a header and a composer, at a height it does not
# choose for itself.
class ChatController < ApplicationController
  def show; end
end
